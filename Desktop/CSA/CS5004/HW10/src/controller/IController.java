package controller;

/**
 * The interface representing a controller that processes commands for image manipulation.
 */
public interface IController {
  /**
   * Starts the image manipulation process by processing commands from the input source.
   */
  void start();
}
